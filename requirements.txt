# requirements.txt

# --- Core Application and Data Handling ---
streamlit>=1.30.0    # The main framework for the UI
pandas>=2.0.0        # Essential for all data loading and manipulation (Data Manager)
numpy>=1.26.0        # Core library for numerical operations, used by pandas and sklearn

# --- Visualization (For Exploration and Analysis Sections) ---
plotly>=5.17.0       # Highly recommended for interactive charts (st.plotly_chart)
matplotlib>=3.8.0    # Standard plotting library
seaborn>=0.13.0      # Library built on Matplotlib for statistical visualization

# --- Machine Learning / Data Preparation (For Data Manager and Analysis) ---
scikit-learn>=1.3.0  # Required for data transformation (scaling, encoding, imputation)
                     # and for analysis techniques like k-means, regression, etc.

# run using steamlit run app.py